name: 🔐 Test Secrets

on:
  push:
    branches: [main]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Check secrets in env
        run: |
          echo "Secrets from env (if any):"
          env | grep SSH || echo "❌ Aucun secret SSH trouvé"
