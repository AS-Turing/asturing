name: 🔐 Test Secrets via Environment

on:
  push:
    branches: [master, main]

jobs:
  debug:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: 🔍 Test if secrets are empty
        run: |
          echo "=== Secrets Check ==="
          [[ -z "${{ secrets.SSH_HOST }}" ]] && echo "❌ SSH_HOST is EMPTY" || echo "✅ SSH_HOST is SET"
          [[ -z "${{ secrets.SSH_USER }}" ]] && echo "❌ SSH_USER is EMPTY" || echo "✅ SSH_USER is SET"
          [[ -z "${{ secrets.DEPLOY_PATH }}" ]] && echo "❌ DEPLOY_PATH is EMPTY" || echo "✅ DEPLOY_PATH is SET"
          [[ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]] && echo "❌ SSH_PRIVATE_KEY is EMPTY" || echo "✅ SSH_PRIVATE_KEY is SET"
