services:
  nuxt:
    image: ghcr.io/as-turing/asturing-nuxt:production
    build:
      context: ./frontend
      dockerfile: Dockerfile
  
  symfony:
    image: ghcr.io/as-turing/asturing-symfony:production
    build:
      context: ./backend
      dockerfile: Dockerfile
